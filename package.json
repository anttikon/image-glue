{
  "name": "image-glue",
  "version": "0.1.20",
  "description": "JavaScript library to combine two images together",
  "main": "build/index.js",
  "keywords": [
    "combine",
    "image"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/anttikon/image-glue"
  },
  "bugs": {
    "url": "https://github.com/anttikon/image-glue/issues"
  },
  "babel": {
    "plugins": [
      "@babel/plugin-transform-runtime"
    ],
    "presets": [
      "@babel/preset-env"
    ]
  },
  "scripts": {
    "compile": "babel ./src -d build && node build/index",
    "prepublish": "npm run compile",
    "test": "jest ."
  },
  "author": "Antti Kontoniemi",
  "license": "MIT",
  "dependencies": {
    "@babel/runtime": "^7.6.3",
    "sharp": "^0.30.5"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/plugin-transform-runtime": "^7.6.2",
    "@babel/preset-env": "^7.6.3",
    "jest": "^24.9.0"
  }
}
